stages:
  - test

test:
  stage: test
  image: python:3.12  # Latest stable Python
  script:
    - pip install -r requirements.txt
    - pip install -e .  # Install package in development mode
    - pytest tests/ -v  # Run all tests
  only:
    - main